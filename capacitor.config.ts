import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.armodilo.app',
  appName: 'bee-connect',
  webDir: 'dist/bee-connect',
  bundledWebRuntime: false
};

export default config;
